## What does this change?

<!-- Describe the change and why it's needed. -->

## How was it tested?

<!-- Which tests did you add or run? -->

## Checklist

- [ ] Tests added or updated for the changed behavior
- [ ] `PYTHONPATH=. pytest` passes locally
- [ ] No secrets, connection strings, or real endpoint hostnames in code, config, or fixtures
- [ ] Seed config in `config/podcast.yaml` still has safe, non-deploying defaults
- [ ] Docs updated if behavior or configuration changed
