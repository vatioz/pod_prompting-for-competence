# Copy this file to .env for local Docker usage.
#
# Configure base URLs, models, prompt templates, and publishing behavior in:
#   config/podcast.yaml
# After first startup, the live runtime copy is:
#   runtime/config/podcast.yaml

AZURE_OPENAI_API_KEY=
AZURE_SPEECH_KEY=
ELEVENLABS_API_KEY=
AZURE_STORAGE_CONNECTION_STRING=
